import widget from './index';

describe('Welcome - Personal Information', () => {
  it('should export the module name', () => {
    expect(widget).toEqual('widget-fob-onboardpersonalinfo-ng');
  });
});
