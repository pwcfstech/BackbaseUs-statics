import widget from './index';

describe('Onboard 2 Reviewinformation', () => {
  it('should export the module name', () => {
    expect(widget).toEqual('widget-fob-onboard-2-reviewinformation-ng');
  });
});
