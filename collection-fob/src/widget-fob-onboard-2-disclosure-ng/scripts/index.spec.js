import widget from './index';

describe('Why we collect this information', () => {
  it('should export the module name', () => {
    expect(widget).toEqual('widget-fob-onboard-2-disclosure-ng');
  });
});
