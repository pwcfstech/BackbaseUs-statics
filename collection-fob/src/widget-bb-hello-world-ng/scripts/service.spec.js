import createService from './service';

describe('HelloWorld::Service', () => {
  // const getService = () =>
  //   createService(null, null);

  beforeEach(function() {
    // ...
  });

  describe('list()', () => {
    it('lists some data', (/* done */) => {
      // getService().list()
      //   .then(function() {
      //     expect(...).toBe(...);
      //   })
      //   .then(done);
    });
  });
});
