import widget from './index';

describe('Capture ID - Back Image', () => {
  it('should export the module name', () => {
    expect(widget).toEqual('widget-fob-onboard-2-capturebackimgid-ng');
  });
});
