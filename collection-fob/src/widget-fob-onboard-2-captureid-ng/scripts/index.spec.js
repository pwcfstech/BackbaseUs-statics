import widget from './index';

describe('Capture Your ID information', () => {
  it('should export the module name', () => {
    expect(widget).toEqual('widget-fob-onboard-2-captureid-ng');
  });
});
