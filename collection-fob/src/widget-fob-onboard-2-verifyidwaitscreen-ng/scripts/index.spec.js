import widget from './index';

describe('Onboard 2 Verifyidwaitscreen', () => {
  it('should export the module name', () => {
    expect(widget).toEqual('widget-fob-onboard-2-verifyidwaitscreen-ng');
  });
});
